<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type loki
  url http://loki:3100
  extra_labels {"job":"fluentd"}
  <label>
    fluentd_worker
  </label>
  <buffer>
    flush_interval 10s
    flush_at_shutdown true
  </buffer>
</match>